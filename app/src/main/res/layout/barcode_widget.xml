<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/widgetbackground"
    style="@style/Widget.ReceiptWidget.AppWidget.Container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/system_accent2_100"
    android:gravity="center_horizontal"
    android:padding="0dp"
    android:paddingBottom="0dp"
    android:theme="@style/Theme.ReceiptWidget.AppWidgetContainer"
    android:visibility="visible">

    <LinearLayout
        android:layout_width="390dp"
        android:layout_height="105dp"
        android:layout_centerInParent="true"
        android:layout_marginStart="0dp"
        android:layout_marginTop="0dp"
        android:layout_marginEnd="0dp"
        android:layout_marginBottom="0dp"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="0dp"
        android:paddingBottom="0dp">

        <ImageView
            android:id="@+id/barcodeImage"
            android:layout_width="800dp"
            android:layout_height="75dp"
            android:layout_marginTop="0dp"
            android:layout_marginEnd="0dp"
            android:contentDescription="@string/barcodeimage"
            android:padding="2dp"
            app:srcCompat="@drawable/app_widget_background" />

        <TextView
            android:id="@+id/barcodeText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:layout_marginBottom="0dp"
            android:padding="0dp"
            android:paddingBottom="0dp"
            android:text="@string/barcodetext"
            android:textAlignment="center"
            android:textColor="@android:color/system_neutral2_800"
            android:textSize="14sp"
            android:visibility="visible" />
    </LinearLayout>

</RelativeLayout>